# Frontend environment variables (Vite).
# Copy to .env.local for local dev, or set in Vercel Project Settings → Environment Variables.

# Base URL of the FastAPI backend. Leave unset locally to use http://localhost:8000.
# In production, point this at your deployed backend (e.g. Render):
# VITE_API_URL=https://nba-player-predictor-api.onrender.com
