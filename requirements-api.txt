# Slim dependency set for the deployed FastAPI backend (Render/Railway/etc).
# Excludes dev/notebook tooling (jupyter, matplotlib, seaborn) to keep the
# server image small and the build fast. For local dev/training use requirements.txt.

# Data
pandas>=2.0
numpy>=1.24,<2.5   # numba (via shap) requires NumPy <= 2.4
scipy>=1.11
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
nba_api>=1.4

# ML
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Utilities
python-dotenv>=1.0
