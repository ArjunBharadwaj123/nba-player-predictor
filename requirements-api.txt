# Slim dependency set for the deployed FastAPI backend (Render/Railway/etc).
# Excludes dev/notebook tooling (jupyter, matplotlib, seaborn) to keep the
# server image small and the build fast. For local dev/training use requirements.txt.

# Data
pandas>=2.0
numpy>=1.24,<2.5   # numba (via shap) requires NumPy <= 2.4
scipy>=1.11
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
nba_api>=1.4

# NFL serving: read the compact parquet serving artifacts + resolve the next
# game's live schedule. Training-only tooling (optuna/jupyter/matplotlib) stays
# out of this file to keep the deployed image small.
pyarrow>=15.0
polars>=1.0
nflreadpy>=0.1.5

# ML
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Utilities
python-dotenv>=1.0
